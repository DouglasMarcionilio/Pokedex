import React from "react";
import { StyleSheet } from "react-native";

const pokelistStyle = StyleSheet.create({
    listContainer: {
        flex: 1,
        justifyContent:"space-around",
        alignItems: "center",
        alignContent: "space-around",
        marginTop: 8,
    },
});

export default pokelistStyle;